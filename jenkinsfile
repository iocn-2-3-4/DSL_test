pipeline {
    agent any
   // publishers {
     //   joinTrigger {
       //     projects('create_jobs')
        //}
    //}
    
    triggers {
        buildResult() {
            combinedJobs()
            triggerInfo('create_jobs', BuildResult.SUCCESS)
        }
    }

    stages {
        stage('Build') {
            steps {
                build job: 'build'
            }
        }
        stage('Test') {
            steps {
                build job: 'test'
                buildButton()
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
