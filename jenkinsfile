pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                build job: 'build'
            }
        }
        stage('Test') {
            steps {
                build job: 'test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
